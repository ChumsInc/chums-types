openapi: 3.0.3
info:
  title: Chums Types
  version: n/a
  x-comment: >-
    Generated by core-types-json-schema
    (https://github.com/grantila/core-types-json-schema) on behalf of typeconv
    (https://github.com/grantila/typeconv)
paths: {}
components:
  schemas:
    CookieConsentSection:
      enum:
        - functional
        - preferences
        - analytics
        - marketing
      title: CookieConsentSection
      type: string
    CookieConsentStatus:
      enum:
        - accepted
        - partial
        - declined
        - pending
      title: CookieConsentStatus
      type: string
    CookieConsentBody:
      properties:
        accepted:
          items:
            $ref: '#/components/schemas/CookieConsentSection'
            title: CookieConsentBody.accepted.[]
          title: CookieConsentBody.accepted
          type: array
        rejected:
          items:
            $ref: '#/components/schemas/CookieConsentSection'
            title: CookieConsentBody.rejected.[]
          title: CookieConsentBody.rejected
          type: array
      required:
        - accepted
        - rejected
      additionalProperties: false
      title: CookieConsentBody
      type: object
    CookieConsentChange:
      allOf:
        - $ref: '#/components/schemas/CookieConsentBody'
        - properties:
            timestamp:
              title: CookieConsentChange.timestamp
              type: string
            url:
              title: CookieConsentChange.url
              type: string
            method:
              title: CookieConsentChange.method
              type: string
          required:
            - timestamp
            - url
            - method
          additionalProperties: false
          type: object
      title: CookieConsentChange
    CookieConsentSettings:
      properties:
        functional:
          title: CookieConsentSettings.functional
          type: boolean
        preferences:
          title: CookieConsentSettings.preferences
          type: boolean
        analytics:
          title: CookieConsentSettings.analytics
          type: boolean
        marketing:
          title: CookieConsentSettings.marketing
          type: boolean
      required:
        - functional
        - preferences
        - analytics
        - marketing
      additionalProperties: false
      title: CookieConsentSettings
      type: object
    CookieConsentRecord:
      properties:
        uuid:
          title: CookieConsentRecord.uuid
          type: string
        userId:
          title: CookieConsentRecord.userId
          nullable: true
          anyOf:
            - type: string
            - type: number
        ipAddress:
          title: CookieConsentRecord.ipAddress
          type: string
        ack:
          title: CookieConsentRecord.ack
          type: boolean
        url:
          title: CookieConsentRecord.url
          type: string
        preferences:
          $ref: '#/components/schemas/CookieConsentSettings'
          title: CookieConsentRecord.preferences
        gpc:
          title: CookieConsentRecord.gpc
          type: boolean
        changes:
          items:
            $ref: '#/components/schemas/CookieConsentChange'
            title: CookieConsentRecord.changes.[]
          title: CookieConsentRecord.changes
          type: array
        status:
          $ref: '#/components/schemas/CookieConsentStatus'
          title: CookieConsentRecord.status
        dateCreated:
          title: CookieConsentRecord.dateCreated
          type: string
        dateUpdated:
          title: CookieConsentRecord.dateUpdated
          type: string
        dateExpires:
          title: CookieConsentRecord.dateExpires
          type: string
      required:
        - uuid
        - userId
        - ipAddress
        - ack
        - url
        - preferences
        - gpc
        - changes
        - status
        - dateCreated
        - dateUpdated
        - dateExpires
      additionalProperties: false
      title: CookieConsentRecord
      type: object
    CookieConsentSectionInfo:
      properties:
        title:
          title: CookieConsentSectionInfo.title
          type: string
        required:
          title: CookieConsentSectionInfo.required
          type: boolean
        description:
          title: CookieConsentSectionInfo.description
          type: string
        cookies:
          items:
            title: CookieConsentSectionInfo.cookies.[]
            type: string
          title: CookieConsentSectionInfo.cookies
          type: array
      required:
        - title
        - required
        - description
      additionalProperties: false
      title: CookieConsentSectionInfo
      type: object
    CookieConsentInfo:
      title: CookieConsentInfo
    CookieConsentHistoryRecord:
      allOf:
        - $ref: '#/components/schemas/CookieConsentRecord'
        - properties:
            email:
              title: CookieConsentHistoryRecord.email
              nullable: true
              type: string
            name:
              title: CookieConsentHistoryRecord.name
              nullable: true
              type: string
            accountType:
              title: CookieConsentHistoryRecord.accountType
              nullable: true
              type: number
          required:
            - email
            - name
            - accountType
          additionalProperties: false
          type: object
      title: CookieConsentHistoryRecord
