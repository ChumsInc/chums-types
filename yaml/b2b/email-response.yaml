openapi: 3.0.3
info:
  title: Chums Types
  version: n/a
  x-comment: >-
    Generated by core-types-json-schema
    (https://github.com/grantila/core-types-json-schema) on behalf of typeconv
    (https://github.com/grantila/typeconv)
paths: {}
components:
  schemas:
    EmailResponse:
      properties:
        accepted:
          items:
            title: EmailResponse.accepted.[]
            type: string
          title: EmailResponse.accepted
          type: array
        rejected:
          items:
            title: EmailResponse.rejected.[]
            type: string
          title: EmailResponse.rejected
          type: array
        envelopeTime:
          title: EmailResponse.envelopeTime
          type: number
        messageTime:
          title: EmailResponse.messageTime
          type: number
        messageSize:
          title: EmailResponse.messageSize
          type: number
        response:
          title: EmailResponse.response
          type: string
        envelope:
          properties:
            from:
              title: EmailResponse.envelope.from
              type: string
            to:
              items:
                title: EmailResponse.envelope.to.[]
                type: string
              title: EmailResponse.envelope.to
              type: array
          required:
            - from
            - to
          additionalProperties: false
          title: EmailResponse.envelope
          type: object
        messageId:
          title: EmailResponse.messageId
          type: string
      required:
        - accepted
        - rejected
        - envelopeTime
        - messageTime
        - messageSize
        - response
        - envelope
        - messageId
      additionalProperties: false
      title: EmailResponse
      type: object
