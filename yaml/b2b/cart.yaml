openapi: 3.0.3
info:
  title: Chums Types
  version: n/a
  x-comment: >-
    Generated by core-types-json-schema
    (https://github.com/grantila/core-types-json-schema) on behalf of typeconv
    (https://github.com/grantila/typeconv)
paths: {}
components:
  schemas:
    CartProgress_Cart:
      title: CartProgress_Cart
      enum:
        - 0
      type: number
    CartProgress_Delivery:
      title: CartProgress_Delivery
      enum:
        - 1
      type: number
    CartProgress_Payment:
      title: CartProgress_Payment
      enum:
        - 2
      type: number
    CartProgress_Confirm:
      title: CartProgress_Confirm
      enum:
        - 3
      type: number
    CartProgress:
      anyOf:
        - $ref: '#/components/schemas/CartProgress_Cart'
          title: CartProgress
        - $ref: '#/components/schemas/CartProgress_Delivery'
          title: CartProgress
        - $ref: '#/components/schemas/CartProgress_Payment'
          title: CartProgress
        - $ref: '#/components/schemas/CartProgress_Confirm'
          title: CartProgress
      title: CartProgress
