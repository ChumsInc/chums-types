openapi: 3.0.3
info:
  title: Chums Types
  version: n/a
  x-comment: >-
    Generated by core-types-json-schema
    (https://github.com/grantila/core-types-json-schema) on behalf of typeconv
    (https://github.com/grantila/typeconv)
paths: {}
components:
  schemas:
    Media:
      properties:
        id:
          title: Media.id
          type: string
        alt:
          title: Media.alt
          type: string
        mediaContentType:
          title: Media.mediaContentType
          type: string
        preview:
          $ref: '#/components/schemas/MediaPreview'
          title: Media.preview
        status:
          $ref: '#/components/schemas/MediaStatus'
          title: Media.status
      required:
        - id
        - alt
        - mediaContentType
        - preview
        - status
      additionalProperties: false
      title: Media
      type: object
    MediaPreview:
      properties:
        image:
          title: MediaPreview.image
        status:
          $ref: '#/components/schemas/MediaStatus'
          title: MediaPreview.status
      required:
        - image
        - status
      additionalProperties: false
      title: MediaPreview
      type: object
    MediaStatus:
      enum:
        - FAILED
        - PROCESSING
        - READY
        - UPLOADED
      title: MediaStatus
      type: string
    MediaConnection:
      title: MediaConnection
