openapi: 3.0.3
info:
  title: Chums Types
  version: n/a
  x-comment: >-
    Generated by core-types-json-schema
    (https://github.com/grantila/core-types-json-schema) on behalf of typeconv
    (https://github.com/grantila/typeconv)
paths: {}
components:
  schemas:
    CollectionNode:
      properties:
        id:
          title: CollectionNode.id
          type: string
        handle:
          title: CollectionNode.handle
          type: string
        title:
          title: CollectionNode.title
          type: string
        updatedAt:
          title: CollectionNode.updatedAt
          type: string
        legacyResourceId:
          title: CollectionNode.legacyResourceId
          type: string
        productsCount:
          title: CollectionNode.productsCount
        resourcePublicationsCount:
          title: CollectionNode.resourcePublicationsCount
      required:
        - id
        - handle
        - title
        - updatedAt
        - legacyResourceId
        - productsCount
        - resourcePublicationsCount
      additionalProperties: false
      title: CollectionNode
      type: object
    CollectionEdge:
      properties:
        node:
          $ref: '#/components/schemas/CollectionNode'
          title: CollectionEdge.node
      required:
        - node
      additionalProperties: false
      title: CollectionEdge
      type: object
    PagedCollectionResponse:
      properties:
        collections:
          properties:
            edges:
              items:
                $ref: '#/components/schemas/CollectionEdge'
                title: PagedCollectionResponse.collections.edges.[]
              title: PagedCollectionResponse.collections.edges
              type: array
            pageInfo:
              title: PagedCollectionResponse.collections.pageInfo
          required:
            - edges
            - pageInfo
          additionalProperties: false
          title: PagedCollectionResponse.collections
          type: object
      required:
        - collections
      additionalProperties: false
      title: PagedCollectionResponse
      type: object
    Collection:
      properties:
        gid:
          title: Collection.gid
          type: string
        id:
          title: Collection.id
          type: string
        handle:
          title: Collection.handle
          type: string
        title:
          title: Collection.title
          type: string
        updatedAt:
          title: Collection.updatedAt
          type: string
        productsCount:
          title: Collection.productsCount
          type: number
        publicationsCount:
          title: Collection.publicationsCount
          type: number
      required:
        - gid
        - id
        - handle
        - title
        - updatedAt
        - productsCount
        - publicationsCount
      additionalProperties: false
      title: Collection
      type: object
