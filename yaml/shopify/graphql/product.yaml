openapi: 3.0.3
info:
  title: Chums Types
  version: n/a
  x-comment: >-
    Generated by core-types-json-schema
    (https://github.com/grantila/core-types-json-schema) on behalf of typeconv
    (https://github.com/grantila/typeconv)
paths: {}
components:
  schemas:
    ProductStatus:
      properties:
        id:
          title: ProductStatus.id
          type: string
        handle:
          title: ProductStatus.handle
          type: string
        updatedAt:
          title: ProductStatus.updatedAt
          type: string
      required:
        - id
        - handle
        - updatedAt
      additionalProperties: false
      title: ProductStatus
      type: object
    ProductStatusResponse:
      title: ProductStatusResponse
    ProductNode:
      properties:
        id:
          title: ProductNode.id
          type: string
        legacyResourceId:
          title: ProductNode.legacyResourceId
          type: string
        title:
          title: ProductNode.title
          type: string
        handle:
          title: ProductNode.handle
          type: string
        createdAt:
          title: ProductNode.createdAt
          type: string
        updatedAt:
          title: ProductNode.updatedAt
          type: string
        status:
          title: ProductNode.status
          type: string
        hasOnlyDefaultVariant:
          title: ProductNode.hasOnlyDefaultVariant
          type: boolean
        collections:
          $ref: '#/components/schemas/ProductCollectionsConnection'
          title: ProductNode.collections
        variants:
          title: ProductNode.variants
      required:
        - id
        - legacyResourceId
        - title
        - handle
        - createdAt
        - updatedAt
        - status
        - hasOnlyDefaultVariant
        - collections
        - variants
      additionalProperties: false
      title: ProductNode
      type: object
    ProductCollectionsConnection:
      properties:
        edges:
          items:
            $ref: '#/components/schemas/ProductCollectionEdge'
            title: ProductCollectionsConnection.edges.[]
          title: ProductCollectionsConnection.edges
          type: array
      required:
        - edges
      additionalProperties: false
      title: ProductCollectionsConnection
      type: object
    ProductCollectionEdge:
      properties:
        node:
          $ref: '#/components/schemas/ProductCollectionNode'
          title: ProductCollectionEdge.node
      required:
        - node
      additionalProperties: false
      title: ProductCollectionEdge
      type: object
    ProductCollectionNode:
      properties:
        handle:
          title: ProductCollectionNode.handle
          type: string
      required:
        - handle
      additionalProperties: false
      title: ProductCollectionNode
      type: object
    ProductResponse:
      title: ProductResponse
    ProductImageInfo:
      properties:
        id:
          title: ProductImageInfo.id
          type: string
        featuredImage:
          properties:
            id:
              title: ProductImageInfo.featuredImage.id
              type: string
          required:
            - id
          additionalProperties: false
          title: ProductImageInfo.featuredImage
          type: object
        images:
          properties:
            edges:
              items: {}
              title: ProductImageInfo.images.edges
              type: array
          required:
            - edges
          additionalProperties: false
          title: ProductImageInfo.images
          type: object
      required:
        - id
        - featuredImage
        - images
      additionalProperties: false
      title: ProductImageInfo
      type: object
    ProductImagesResponse:
      title: ProductImagesResponse
