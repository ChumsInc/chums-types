openapi: 3.0.3
info:
  title: Chums Types
  version: n/a
  x-comment: >-
    Generated by core-types-json-schema
    (https://github.com/grantila/core-types-json-schema) on behalf of typeconv
    (https://github.com/grantila/typeconv)
paths: {}
components:
  schemas:
    InventoryItemsResponse:
      properties:
        inventoryItems:
          properties:
            edges:
              items:
                $ref: '#/components/schemas/InventoryItemEdge'
                title: InventoryItemsResponse.inventoryItems.edges.[]
              title: InventoryItemsResponse.inventoryItems.edges
              type: array
            pageInfo:
              title: InventoryItemsResponse.inventoryItems.pageInfo
          required:
            - edges
            - pageInfo
          additionalProperties: false
          title: InventoryItemsResponse.inventoryItems
          type: object
      required:
        - inventoryItems
      additionalProperties: false
      title: InventoryItemsResponse
      type: object
    InventoryItem:
      properties:
        id:
          title: InventoryItem.id
          type: string
        legacyResourceId:
          title: InventoryItem.legacyResourceId
          type: string
        variant:
          title: InventoryItem.variant
        measurement:
          $ref: '#/components/schemas/InventoryItemMeasurement'
          title: InventoryItem.measurement
        provinceCodeOfOrigin:
          title: InventoryItem.provinceCodeOfOrigin
          nullable: true
          type: string
        harmonizedSystemCode:
          title: InventoryItem.harmonizedSystemCode
          nullable: true
          type: string
        inventoryLevel:
          $ref: '#/components/schemas/InventoryLevel'
          title: InventoryItem.inventoryLevel
      required:
        - id
        - legacyResourceId
        - variant
        - measurement
        - provinceCodeOfOrigin
        - harmonizedSystemCode
        - inventoryLevel
      additionalProperties: false
      title: InventoryItem
      type: object
    InventoryItemEdge:
      properties:
        node:
          $ref: '#/components/schemas/InventoryItem'
          title: InventoryItemEdge.node
      required:
        - node
      additionalProperties: false
      title: InventoryItemEdge
      type: object
    InventoryQuantityInput:
      properties:
        inventoryItemId:
          title: InventoryQuantityInput.inventoryItemId
          type: string
        locationId:
          title: InventoryQuantityInput.locationId
          type: string
        quantity:
          title: InventoryQuantityInput.quantity
          type: number
        compareQuantity:
          title: InventoryQuantityInput.compareQuantity
          type: number
      required:
        - inventoryItemId
        - locationId
        - quantity
        - compareQuantity
      additionalProperties: false
      title: InventoryQuantityInput
      type: object
    InventoryLevelChanges:
      properties:
        name:
          enum:
            - available
            - on_hand
          title: InventoryLevelChanges.name
          type: string
        reason:
          enum:
            - correction
            - cycle_count_available
            - damaged
            - movement_created
            - movement_updated
            - movement_received
            - movement_canceled
            - other
          title: InventoryLevelChanges.reason
          type: string
        quantities:
          items:
            $ref: '#/components/schemas/InventoryQuantityInput'
            title: InventoryLevelChanges.quantities.[]
          title: InventoryLevelChanges.quantities
          type: array
      required:
        - name
        - reason
        - quantities
      additionalProperties: false
      title: InventoryLevelChanges
      type: object
    InventoryChange:
      properties:
        name:
          title: InventoryChange.name
          type: string
        delta:
          title: InventoryChange.delta
          type: number
        quantityAfterChange:
          title: InventoryChange.quantityAfterChange
          type: number
      required:
        - name
        - delta
        - quantityAfterChange
      additionalProperties: false
      title: InventoryChange
      type: object
    InventoryAdjustmentGroup:
      properties:
        reason:
          title: InventoryAdjustmentGroup.reason
          type: string
        changes:
          items:
            $ref: '#/components/schemas/InventoryChange'
            title: InventoryAdjustmentGroup.changes.[]
          title: InventoryAdjustmentGroup.changes
          type: array
      required:
        - reason
        - changes
      additionalProperties: false
      title: InventoryAdjustmentGroup
      type: object
    InventoryLevelMutationResponse:
      properties:
        inventorySetQuantities:
          properties:
            inventoryAdjustmentGroup:
              $ref: '#/components/schemas/InventoryAdjustmentGroup'
              title: >-
                InventoryLevelMutationResponse.inventorySetQuantities.inventoryAdjustmentGroup
            userErrors:
              items: {}
              title: InventoryLevelMutationResponse.inventorySetQuantities.userErrors
              type: array
          required:
            - inventoryAdjustmentGroup
            - userErrors
          additionalProperties: false
          title: InventoryLevelMutationResponse.inventorySetQuantities
          type: object
      required:
        - inventorySetQuantities
      additionalProperties: false
      title: InventoryLevelMutationResponse
      type: object
    InventoryItemMeasurement:
      properties:
        id:
          title: InventoryItemMeasurement.id
          type: string
        weight:
          title: InventoryItemMeasurement.weight
      required:
        - id
        - weight
      additionalProperties: false
      title: InventoryItemMeasurement
      type: object
    InventoryLevel:
      properties:
        quantities:
          items:
            $ref: '#/components/schemas/InventoryQuantity'
            title: InventoryLevel.quantities.[]
          title: InventoryLevel.quantities
          type: array
        updatedAt:
          title: InventoryLevel.updatedAt
          type: string
      required:
        - quantities
        - updatedAt
      additionalProperties: false
      title: InventoryLevel
      type: object
    InventoryQuantity:
      properties:
        id:
          title: InventoryQuantity.id
          type: string
        quantity:
          title: InventoryQuantity.quantity
          type: number
      required:
        - id
        - quantity
      additionalProperties: false
      title: InventoryQuantity
      type: object
