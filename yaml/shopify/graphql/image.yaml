openapi: 3.0.3
info:
  title: Chums Types
  version: n/a
  x-comment: >-
    Generated by core-types-json-schema
    (https://github.com/grantila/core-types-json-schema) on behalf of typeconv
    (https://github.com/grantila/typeconv)
paths: {}
components:
  schemas:
    Image:
      properties:
        id:
          title: Image.id
          type: string
        url:
          title: Image.url
          type: string
        width:
          title: Image.width
          nullable: true
          type: number
        height:
          title: Image.height
          nullable: true
          type: number
        altText:
          title: Image.altText
          nullable: true
          type: string
      required:
        - id
        - url
        - width
        - height
        - altText
      additionalProperties: false
      title: Image
      type: object
    ProductImage:
      allOf:
        - $ref: '#/components/schemas/Image'
        - properties:
            product_id:
              title: ProductImage.product_id
              type: string
          required:
            - product_id
          additionalProperties: false
          type: object
      title: ProductImage
    ImageEdge:
      properties:
        node:
          $ref: '#/components/schemas/Image'
          title: ImageEdge.node
      required:
        - node
      additionalProperties: false
      title: ImageEdge
      type: object
