openapi: 3.0.3
info:
  title: Chums Types
  version: 3.0.4
  x-comment: >-
    Generated by core-types-json-schema
    (https://github.com/grantila/core-types-json-schema) on behalf of typeconv
    (https://github.com/grantila/typeconv)
paths: {}
components:
  schemas:
    Tab:
      properties:
        id:
          title: Tab.id
          type: string
        title:
          title: Tab.title
          type: string
        icon:
          title: Tab.icon
          description: Bootstrap icon className
          type: string
        canClose:
          title: Tab.canClose
          type: boolean
        disabled:
          title: Tab.disabled
          type: boolean
      required:
        - id
        - title
      additionalProperties: false
      title: Tab
      type: object
