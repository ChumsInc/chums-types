openapi: 3.0.3
info:
  title: Chums Types
  version: n/a
  x-comment: >-
    Generated by core-types-json-schema
    (https://github.com/grantila/core-types-json-schema) on behalf of typeconv
    (https://github.com/grantila/typeconv)
paths: {}
components:
  schemas:
    MenuItem:
      properties:
        id:
          title: MenuItem.id
          type: number
        menu_text:
          title: MenuItem.menu_text
          type: string
        menu_url:
          title: MenuItem.menu_url
          type: string
        has_children:
          title: MenuItem.has_children
          type: boolean
        menu_id:
          title: MenuItem.menu_id
          type: string
        children:
          items:
            $ref: '#/components/schemas/MenuItem'
            title: MenuItem.children.[]
          title: MenuItem.children
          type: array
      required:
        - id
        - menu_text
        - menu_url
        - has_children
        - menu_id
      additionalProperties: false
      title: MenuItem
      type: object
    MenuItemEntry:
      properties:
        parent_menu:
          title: MenuItemEntry.parent_menu
          type: number
        priority:
          title: MenuItemEntry.priority
          type: number
        active:
          title: MenuItemEntry.active
          type: boolean
        required_role:
          title: MenuItemEntry.required_role
          type: string
        timestamp:
          title: MenuItemEntry.timestamp
          type: string
      required:
        - parent_menu
        - priority
        - active
        - required_role
        - timestamp
      additionalProperties: false
      title: MenuItemEntry
      type: object
    UserFavorite:
      properties:
        id:
          title: UserFavorite.id
          type: number
        path:
          title: UserFavorite.path
          type: string
        name:
          title: UserFavorite.name
          type: string
      required:
        - id
        - path
        - name
      additionalProperties: false
      title: UserFavorite
      type: object
