openapi: 3.0.3
info:
  title: Chums Types
  version: 3.0.4
  x-comment: >-
    Generated by core-types-json-schema
    (https://github.com/grantila/core-types-json-schema) on behalf of typeconv
    (https://github.com/grantila/typeconv)
paths: {}
components:
  schemas:
    Editable:
      properties:
        changed:
          title: Editable.changed
          type: boolean
      additionalProperties: false
      title: Editable
      type: object
    LoadingStatus:
      enum:
        - idle
        - pending
        - succeeded
        - failed
      title: LoadingStatus
      type: string
    YesNo:
      enum:
        - 'Y'
        - 'N'
      title: YesNo
      type: string
